#pragma once

namespace IED
{
	/*struct objectEntrySlot_t;

	struct SlotDeferedCleanupObject
	{
		bool Queue(
			Game::ObjectRefHandle a_handle,
			const objectEntrySlot_t& a_object);

		void TryCleanup(Game::ObjectRefHandle a_handle);
		void ForceCleanup(Game::ObjectRefHandle a_handle);

		inline constexpr explicit operator bool() const noexcept
		{
			return object != nullptr;
		}

		NiPointer<NiNode> object;
		long long timeout{ std::numeric_limits<long long>::min() };
		Biped::BIPED_OBJECT biped{ Biped::BIPED_OBJECT::kNone };
	};*/
}